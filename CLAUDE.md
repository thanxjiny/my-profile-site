@README.md

## 프로젝트 구조

- 소스 파일: `index.html` 단 하나 (모든 섹션 포함)
- 빌드 시스템 없음 — package.json 없음

## 작업 시 주의사항

- CSS는 Tailwind CDN 클래스 기반 — 별도 CSS 파일 없음
- 외부 라이브러리는 모두 CDN으로 로드 (npm 설치 불필요)
- 섹션 ID: `#about`, `#skills`, `#projects`, `#contact`
